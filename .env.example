# Copy to .env on the server. Nothing here is secret, but the address matters:
# canonical URLs, the sitemap and link previews are all built from it, so a
# wrong value gets indexed as a broken link.

NEXT_PUBLIC_SITE_URL=https://your-domain.example

# Port to listen on. Pick one nothing else on the server is using.
PORT=3001

NODE_ENV=production
